%h1 Listing items
= link_to 'New Item', new_item_path, role: "button", class:"btn btn-default"
%table
  %thead
    %tr
      - if admin?
        %th Available
      %th Name
      %th Description
      %th Price
      - if admin?
        %th Cost
      %th Category
      - if admin?
        %th Sort order
      %th
      %th
      %th
  %tbody
    - @items.each do |item|
      = render :partial => 'items/listing', :locals => { :item => item }
%br/
