%h1 Listing items
%table
  %thead
    %tr
      %th Available
      %th Name
      %th Description
      %th Price
      %th Cost
      %th Category
      %th Sort order
      %th
      %th
      %th
  %tbody
    - @items.each do |item|
      = render :partial => 'items/listing', :locals => { :item => item }
%br/
= link_to 'New Item', new_item_path
