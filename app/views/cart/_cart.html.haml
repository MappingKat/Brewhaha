%ul.cart-item
  %li
    %table.table
      %thead
        %tr
          %th
          %th Qt.
          %th
          %th
          %th Beverage
          %th Price
          - current_cart.cart_items.each do |cart_item|
            = render :partial => 'cart/cart_item', :locals => { :cart_item => cart_item }
  %li
    Total
  %li.divider
  %li
    = link_to 'Send Order', '#', role: "button", class:"btn btn-default"
